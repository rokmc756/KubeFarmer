---
base_path: /root

postgres:
  install: true
  major_version: 10
  minor_version: 1
  patch_version: 0
  build_version:
  clone_git: false
  nvme: false
  lb_ip_addr: "192.168.1.212"
  storage_path: "/mnt/postgres-storage"
  replica: 3
  db:
    name: "powerdns"
    user: "jomoon"
    password: "Changeme1234"
  root_password: "Changeme1234"
  mariabackup_password: "Changeme1234"
  k8s:
    major_version: 1
    minor_version: 30
    patch_version: 0
    build_version:
  hostname: "postgres.jtest.pivotal.io"
  service:
    metadata:
      name: postgres-loadbalancer
      namespace: postgres
  ingress:
    metadata:
      name: postgres-ingress
      namespace: nginx-ingress
    hostname: "postgres.jtest.pivotal.io"
  kernel_parameters:
    - "net.ipv4.neigh.default.gc_thresh1 = 0"       # For ARP Cache
  manager:
    install: false
  lb:
    install: true


phpmyadmin:
  install: true
  lb_ip_addr: "192.168.1.237"
  storage_path: "/mnt/phpmyadmin-storage"
  replica: 1
  db:
    name: "powerdns"
    user: "jomoon"
    password: "Changeme1234"
  root_password: "Changeme1234"
  mariabackup_password: "Changeme1234"
  hostname: "phpmyadmin.jtest.pivotal.io"
  service:
    metadata:
      name: phpmyadmin-loadbalancer
      namespace: phpmyadmin
  ingress:
    metadata:
      name: phpmyadmin-ingress
      namespace: nginx-ingress
    hostname: "phpmyadmin.jtest.pivotal.io"
  kernel_parameters:
    - "net.ipv4.neigh.default.gc_thresh1 = 0"       # For ARP Cache
  lb:
    install: true


helm:
  enable_repo: true
  major_version: 3
  minor_version:
  patch_version:
  build_version:


cert_manager:
  install: false
  major_version: 1
  minor_version: 7
  patch_version: 1

