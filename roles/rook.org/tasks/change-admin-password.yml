
# kubectl create -f rook/deploy/examples/toolbox.yaml
# kubectl -n rook-ceph exec -it deploy/rook-ceph-tools -- bash
# echo 'Changeme34#$' > /tmp/passwd
# ceph dashboard ac-user-create admin -i /tmp/passwd administrator

